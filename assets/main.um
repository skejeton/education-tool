fn button(text: str): bool
fn label(text: str, color: uint32)
fn nextrow()

interactions := ""

fn main() {}

fn onInteract*(id: str) {
    interactions += id+"\n"
}

fn doUI*() {
    label(sprintf("Interactions: %v", interactions), 0x000000FF)
}
